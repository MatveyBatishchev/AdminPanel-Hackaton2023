import React from 'react';
import classes from './style.module.scss';

const Welcome = () => {
    return (
        <>
            <h1 className={classes['title']}>Привет!</h1>
        </>
    );
};

export default Welcome;